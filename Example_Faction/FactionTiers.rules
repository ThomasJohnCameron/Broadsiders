//Uhhhhhh your guess is as good as mine, if i had to assume, sets the system tiers allowing for systems to go from 1-3 to 16-18
FactionTiers
{
	Type = FactionNodeTiers
	Factions
	[
		{
			FactionID = faction1
			TierRangeLow = [1, (&~/MaxTier) - (&~/TierSpread)]
			TierRangeHigh = [1 + (&~/TierSpread), (&~/MaxTier)]
		}
		//Faction 2, To "turn on" the second faction just remove the "//" on the left here
//		{
//			FactionID = faction2
//			TierRangeLow = [1, (&~/MaxTier) - (&~/TierSpread)]
//			TierRangeHigh = [1 + (&~/TierSpread), (&~/MaxTier)]
//		}
	]
}